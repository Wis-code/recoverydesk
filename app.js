import { initializeApp } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js";
import { getAuth, onAuthStateChanged, signInWithEmailAndPassword, signInWithPopup, signInWithRedirect, getRedirectResult, GoogleAuthProvider, signOut } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";
import { getDatabase, ref, get, onValue, push, set, update, runTransaction } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-database.js";

const firebaseConfig={apiKey:"AIzaSyBfBsu81ykk11hz-jX0_E6TrAriQCTZYJ8",authDomain:"wiscodery-forensic.firebaseapp.com",databaseURL:"https://wiscodery-forensic-default-rtdb.europe-west1.firebasedatabase.app",projectId:"wiscodery-forensic",storageBucket:"wiscodery-forensic.firebasestorage.app",messagingSenderId:"1032205659317",appId:"1:1032205659317:web:4ba6efda150b1bab64e2fc",measurementId:"G-CK43JWDQ5C"};
const fb=initializeApp(firebaseConfig),auth=getAuth(fb),db=getDatabase(fb),provider=new GoogleAuthProvider();provider.setCustomParameters({prompt:"select_account"});
const app=document.getElementById("app");let state={user:null,profile:null,jobs:[],view:"dashboard",selectedJob:null,loading:true};
const esc=(v="")=>String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));const money=v=>Number(v||0).toLocaleString();
function applyTheme(){const t=localStorage.getItem("rd-theme")||"system";document.documentElement.dataset.theme=(t==="dark"||(t==="system"&&matchMedia("(prefers-color-scheme: dark)").matches))?"dark":"light"}applyTheme();
getRedirectResult(auth).catch(console.error);
onAuthStateChanged(auth,async user=>{state.user=user;state.profile=null;state.loading=false;if(user){const s=await get(ref(db,`users/${user.uid}`));if(s.exists())state.profile={id:user.uid,...s.val()}}if(user&&state.profile)subscribeJobs();render()});
function subscribeJobs(){onValue(ref(db,"jobs"),s=>{const raw=s.val()||{};state.jobs=Object.entries(raw).map(([id,v])=>({id,...v})).sort((a,b)=>(b.createdAt||0)-(a.createdAt||0));render()})}
function render(){if(state.loading){app.innerHTML=`<div class="login-page">Loading RecoveryDesk…</div>`;return}if(!state.user)return renderLogin();if(!state.profile)return renderUnauthorized();renderShell()}
function renderLogin(){app.innerHTML=`<div class="login-page"><form class="auth-card" id="loginForm"><div class="brand"><div class="brand-mark">R</div><div><strong>RecoveryDesk</strong><div class="muted tiny">Wiscodery Forensic</div></div></div><span class="eyebrow">STAFF PORTAL</span><h1>Sign in</h1><p class="muted">Manage customer onboarding and recovery jobs.</p><div id="loginError" class="alert error hidden"></div><button type="button" class="secondary full" id="googleBtn">G &nbsp; Continue with Google</button><div class="divider"><span>or</span></div><label>Email<input id="email" type="email" required autocomplete="email"></label><label>Password<input id="password" type="password" required autocomplete="current-password"></label><button class="primary full">Sign in</button></form></div>`;document.getElementById("loginForm").onsubmit=async e=>{e.preventDefault();const er=document.getElementById("loginError");try{await signInWithEmailAndPassword(auth,document.getElementById("email").value,document.getElementById("password").value)}catch{er.textContent="Could not sign in. Check the email and password.";er.classList.remove("hidden")}};document.getElementById("googleBtn").onclick=async()=>{const er=document.getElementById("loginError");try{await signInWithPopup(auth,provider)}catch(err){if(["auth/popup-blocked","auth/cancelled-popup-request","auth/operation-not-supported-in-this-environment"].includes(err.code))await signInWithRedirect(auth,provider);else{er.textContent="Google sign-in could not be completed.";er.classList.remove("hidden")}}}}
function renderUnauthorized(){app.innerHTML=`<div class="login-page"><div class="auth-card"><span class="eyebrow">ACCOUNT NOT AUTHORIZED</span><h2>Staff access required</h2><p>This account signed in successfully, but it is not listed under <code>/users</code> in RecoveryDesk.</p><button id="logoutBtn" class="secondary full">Sign out</button></div></div>`;document.getElementById("logoutBtn").onclick=()=>signOut(auth)}
function navButton(v,l){return `<button data-view="${v}" class="${state.view===v?"active":""}">${l}</button>`}
function renderShell(){const admin=state.profile.role==="admin";app.innerHTML=`<div class="app-shell"><aside class="sidebar no-print"><div class="brand"><div class="brand-mark">R</div><div><strong>RecoveryDesk</strong><div class="muted tiny">Wiscodery Forensic</div></div></div><div class="side-nav">${navButton("dashboard","Dashboard")}${navButton("jobs","Jobs")}${navButton("new","New Job")}${admin?navButton("staff","Staff"):""}${admin?navButton("settings","Settings"):""}</div><div class="sidebar-footer"><div><strong>${esc(state.profile.name||"User")}</strong><div class="muted tiny">${esc(state.profile.role)}</div></div><button class="secondary" id="logoutBtn">Sign out</button></div></aside><main class="main"><div class="toolbar no-print"><select id="themeSelect"><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></div><div class="content" id="view"></div><div class="bottom-nav no-print">${navButton("dashboard","⌂ Dashboard")}${navButton("jobs","▤ Jobs")}${navButton("new","+ New")}${admin?navButton("settings","⚙ Settings"):""}</div></main></div>`;document.querySelectorAll("[data-view]").forEach(b=>b.onclick=()=>{state.view=b.dataset.view;state.selectedJob=null;renderShell()});document.getElementById("logoutBtn").onclick=()=>signOut(auth);const ts=document.getElementById("themeSelect");ts.value=localStorage.getItem("rd-theme")||"system";ts.onchange=()=>{localStorage.setItem("rd-theme",ts.value);applyTheme()};renderView()}
function renderView(){const el=document.getElementById("view");if(state.view==="dashboard")return renderDashboard(el);if(state.view==="jobs")return renderJobs(el);if(state.view==="new")return renderNew(el);if(state.view==="staff")return renderStaff(el);if(state.view==="settings")return renderSettings(el);if(state.view==="job")return renderJob(el,state.selectedJob)}
function jobsHtml(list){if(!list.length)return `<p class="muted">No jobs yet.</p>`;return `<div class="jobs">${list.map(j=>`<button class="job" data-job="${j.id}"><div><strong>${esc(j.jobId)}</strong><br><small>${esc(j.clientName)} · ${esc(j.phone)}</small></div><div class="device"><strong>${esc(j.deviceType)}</strong><br><small>${esc(j.brandModel||j.capacity||"")}</small></div><span class="status">${esc(j.status)}</span></button>`).join("")}</div>`}
function bindJobClicks(){document.querySelectorAll("[data-job]").forEach(b=>b.onclick=()=>{state.selectedJob=state.jobs.find(j=>j.id===b.dataset.job);state.view="job";renderShell()})}
function renderDashboard(el){const a=state.jobs.filter(j=>j.status==="Assessment").length,act=state.jobs.filter(j=>["Awaiting Approval","Recovery In Progress"].includes(j.status)).length,r=state.jobs.filter(j=>j.status==="Ready for Collection").length;el.innerHTML=`<div class="page-head"><div><span class="eyebrow">OVERVIEW</span><h1>Good day, ${esc((state.profile.name||"there").split(" ")[0])}.</h1><p class="muted">Recovery jobs at a glance.</p></div><button class="primary no-print" id="newBtn">+ New job</button></div><div class="stats"><div class="stat"><span>Total jobs</span><strong>${state.jobs.length}</strong></div><div class="stat"><span>Assessment</span><strong>${a}</strong></div><div class="stat"><span>Active recovery</span><strong>${act}</strong></div><div class="stat"><span>Ready</span><strong>${r}</strong></div></div><div class="panel"><h2>Recent jobs</h2>${jobsHtml(state.jobs.slice(0,6))}</div>`;document.getElementById("newBtn").onclick=()=>{state.view="new";renderShell()};bindJobClicks()}
function renderJobs(el){el.innerHTML=`<div class="page-head"><div><span class="eyebrow">RECOVERY WORK</span><h1>Jobs</h1></div><div class="no-print"><button class="secondary" id="exportBtn">Export CSV</button><button class="primary" id="newBtn">+ New job</button></div></div><div class="panel"><input id="search" placeholder="Search job ID, customer, phone or device…"><div id="jobList">${jobsHtml(state.jobs)}</div></div>`;bindJobClicks();document.getElementById("newBtn").onclick=()=>{state.view="new";renderShell()};document.getElementById("search").oninput=e=>{const q=e.target.value.toLowerCase();const f=state.jobs.filter(j=>[j.jobId,j.clientName,j.phone,j.deviceType,j.brandModel,j.status].join(" ").toLowerCase().includes(q));document.getElementById("jobList").innerHTML=jobsHtml(f);bindJobClicks()};document.getElementById("exportBtn").onclick=exportCSV}
const field=(l,n,req=false,t="text")=>`<label class="field"><span>${l}</span><input type="${t}" name="${n}" ${req?"required":""}></label>`;const area=(l,n,req=false)=>`<label class="field"><span>${l}</span><textarea name="${n}" ${req?"required":""}></textarea></label>`;const check=(n,l)=>`<label class="check-row"><input type="checkbox" name="${n}"><span>${l}</span></label>`;
function renderNew(el){el.innerHTML=`<div class="page-head"><div><span class="eyebrow">CUSTOMER INTAKE</span><h1>New recovery job</h1><p class="muted">Customer, device and assessment authorization.</p></div></div><form id="jobForm"><div class="panel"><h2>Customer</h2><div class="form-grid">${field("Full name *","clientName",true)}${field("Phone number *","phone",true)}${field("Email","email",false,"email")}${field("Address / area","address")}</div></div><div class="panel"><h2>Device</h2><div class="form-grid three"><label class="field"><span>Device type</span><select name="deviceType"><option>Hard Disk Drive (HDD)</option><option>Solid State Drive (SSD)</option><option>SD Card</option><option>MicroSD Card</option><option>USB Flash Drive</option><option>Other</option></select></label>${field("Brand / model","brandModel")}${field("Capacity","capacity")}${field("Serial number","serial")}${field("Physical condition","condition")}<label class="field"><span>Previous recovery attempt?</span><select name="previousAttempt"><option>No</option><option>Yes</option><option>Unknown</option></select></label></div>${area("Problem reported *","problem",true)}${area("Important data requested","requestedData")}</div><div class="panel"><h2>Assessment & payment</h2><div class="form-grid three">${field("Assessment fee (₦)","assessmentFee",false,"number")}<label class="field"><span>Payment method</span><select name="assessmentPayment"><option>Not Paid</option><option>Cash</option><option>Bank Transfer</option><option>POS</option><option>Waived</option></select></label><label class="field"><span>Assessment result</span><select name="assessmentResult"><option>Pending</option><option>Recovery Appears Feasible</option><option>Not Recoverable</option><option>Further Assessment Required</option></select></label></div><div class="notice">The assessment fee covers diagnosis and checking whether recovery appears feasible. It does not guarantee that all or any specific files can be recovered.</div></div><div class="panel"><h2>Customer authorization</h2>${check("ownsDevice","I confirm that I own this device or have lawful authority from the owner to request data recovery.")}${check("authorizeAssessment","I authorize Wiscodery Forensic to inspect, connect, test and diagnose this device for the purpose of assessing data recovery.")}${check("riskAccepted","I understand that damaged or unstable storage devices may deteriorate or fail during reasonable assessment or recovery attempts, and that recovery cannot be guaranteed.")}${check("privacyAccepted","I understand that limited access to files may be necessary to verify recovered data, and I authorize such access only for providing this service.")}${field("Customer confirmation name *","confirmationName",true)}<p class="tiny muted">For walk-in customers, save the job then print the authorization page for a physical signature.</p></div><div class="panel"><h2>Staff notes</h2>${area("Internal notes","notes")}</div><div id="formError" class="alert error hidden"></div><button class="primary no-print">Save job</button></form>`;document.getElementById("jobForm").onsubmit=createJob}
async function createJob(e){e.preventDefault();const f=new FormData(e.target);for(const n of ["ownsDevice","authorizeAssessment","riskAccepted","privacyAccepted"]){if(!f.get(n)){const er=document.getElementById("formError");er.textContent="Customer must accept all authorization statements.";er.classList.remove("hidden");return}}const y=new Date().getFullYear();const cr=ref(db,`counters/jobs-${y}`);const result=await runTransaction(cr,c=>(Number(c)||0)+1);const jobId=`DR-${y}-${String(result.snapshot.val()).padStart(4,"0")}`;const jr=push(ref(db,"jobs"));const obj=Object.fromEntries(f.entries());Object.assign(obj,{jobId,status:"Received",createdBy:state.user.uid,createdByEmail:state.user.email||"",createdAt:Date.now(),updatedAt:Date.now()});await set(jr,obj);state.view="jobs";renderShell()}
function renderJob(el,j){if(!j){state.view="jobs";renderShell();return}el.innerHTML=`<div class="page-head"><div><span class="eyebrow">JOB ${esc(j.jobId)}</span><h1>${esc(j.clientName)}</h1><p class="muted">${esc(j.deviceType)}</p></div><div class="no-print"><button class="secondary" id="printBtn">Print customer form</button><button class="secondary" id="backBtn">Back</button></div></div><div class="panel"><div class="form-grid"><div><strong>Phone</strong><p>${esc(j.phone)}</p></div><div><strong>Email</strong><p>${esc(j.email||"—")}</p></div><div><strong>Serial</strong><p>${esc(j.serial||"—")}</p></div><div><strong>Assessment fee</strong><p>₦${money(j.assessmentFee)}</p></div></div></div><div class="panel"><h2>Reported issue</h2><p>${esc(j.problem||"—")}</p><h3>Important data requested</h3><p>${esc(j.requestedData||"—")}</p></div><div class="panel"><h2>Customer authorization</h2><p>☑ I confirm that I own this device or have lawful authority from the owner.</p><p>☑ I authorize Wiscodery Forensic to inspect, connect, test and diagnose the device.</p><p><strong>Risk notice:</strong> Damaged or unstable storage devices may deteriorate or fail during reasonable assessment or recovery attempts, and recovery cannot be guaranteed.</p><div class="signature-grid"><div>Customer: <strong>${esc(j.confirmationName||j.clientName)}</strong></div><div>Customer signature<div class="signature-line"></div></div><div>Date<div class="signature-line"></div></div><div>Staff signature<div class="signature-line"></div></div></div></div><div class="panel no-print"><h2>Job controls</h2><div class="form-grid three"><label class="field"><span>Status</span><select id="statusSel">${["Received","Assessment","Awaiting Approval","Recovery In Progress","Completed","Ready for Collection","Closed"].map(x=>`<option ${j.status===x?"selected":""}>${x}</option>`).join("")}</select></label><label class="field"><span>Assessment result</span><select id="resultSel">${["Pending","Recovery Appears Feasible","Not Recoverable","Further Assessment Required"].map(x=>`<option ${j.assessmentResult===x?"selected":""}>${x}</option>`).join("")}</select></label><label class="field"><span>Recovery quote (₦)</span><input id="quote" type="number" value="${esc(j.recoveryQuote||"")}"></label></div><label class="field"><span>Staff notes</span><textarea id="notes">${esc(j.notes||"")}</textarea></label><button class="primary" id="saveChanges">Save changes</button></div>`;document.getElementById("printBtn").onclick=()=>window.print();document.getElementById("backBtn").onclick=()=>{state.view="jobs";state.selectedJob=null;renderShell()};document.getElementById("saveChanges").onclick=async()=>{const u={status:document.getElementById("statusSel").value,assessmentResult:document.getElementById("resultSel").value,recoveryQuote:document.getElementById("quote").value,notes:document.getElementById("notes").value,updatedAt:Date.now()};await update(ref(db,`jobs/${j.id}`),u);Object.assign(j,u);renderShell()}}
async function renderStaff(el){const s=await get(ref(db,"users")),raw=s.val()||{};el.innerHTML=`<div class="page-head"><div><span class="eyebrow">ADMIN</span><h1>Staff</h1></div></div><div class="panel"><div class="notice">For now, staff accounts are added through Firebase Authentication and Realtime Database.</div>${Object.entries(raw).map(([uid,u])=>`<p><strong>${esc(u.name||"Unnamed")}</strong> — ${esc(u.role||"worker")}<br><small>${esc(u.email||uid)}</small></p>`).join("")}</div>`}
function renderSettings(el){el.innerHTML=`<div class="page-head"><div><span class="eyebrow">ADMIN</span><h1>Settings</h1></div></div><div class="panel"><h2>Appearance</h2><p class="muted">Use the theme selector on desktop. Mobile keeps your saved preference.</p></div><div class="panel"><h2>Next planned modules</h2><p class="muted">Service agreement, quotation approval, completion/handover, signed-paper attachment and Google Sheets reporting.</p></div>`}
function exportCSV(){const h=["Job ID","Created","Status","Customer","Phone","Email","Device Type","Brand/Model","Capacity","Serial","Problem","Requested Data","Assessment Fee","Payment","Assessment Result","Recovery Quote","Notes"];const rows=state.jobs.map(j=>[j.jobId,j.createdAt?new Date(j.createdAt).toISOString():"",j.status,j.clientName,j.phone,j.email,j.deviceType,j.brandModel,j.capacity,j.serial,j.problem,j.requestedData,j.assessmentFee,j.assessmentPayment,j.assessmentResult,j.recoveryQuote,j.notes]);const csv=[h,...rows].map(r=>r.map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(",")).join("\n");const b=new Blob([csv],{type:"text/csv;charset=utf-8"}),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download=`recoverydesk-backup-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(u)}
if("serviceWorker" in navigator&&location.protocol==="https:"){addEventListener("load",()=>navigator.serviceWorker.register("./sw.js").catch(console.error))}
